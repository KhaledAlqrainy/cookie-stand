'use strict';

function randomValue(min, max) {
    return Math.floor(Math.random() * (max - min + 1) ) + min;
}


const seattle = {

    mincust = 23,
    maxcust = 65,
    avgcookie = 6.3,
    arr:[],
     randomnum: function(){
         this.arr.push( math.floor(math.random()*(this.mincust - this.maxcust +1))+this.mincust);
       console.log(this.arr);  
     }
    }
    seattle.randomnum();
    

   



    



